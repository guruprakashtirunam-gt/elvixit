// Backend API base URL configuration.
//
// When frontend and backend are deployed as separate Render services,
// the frontend needs to know the backend's URL to call the API.
//
// - Local development (backend running on localhost:5000): leave as ''
//   (relative URLs) or set to 'http://127.0.0.1:5000'.
// - Production on Render: set this to your deployed backend's URL,
//   e.g. 'https://your-backend.onrender.com' (no trailing slash).
window.API_BASE_URL = '';
